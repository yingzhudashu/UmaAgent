# 会话修复发布验收（2026-09-21）

本候选为 Core 1.3.0、Protocol v16、schema 27、Android 1.4.2（16）。

本轮修复活动路径历史、编辑覆盖、质量结果恢复、运行取消隔离和队列排序版本冲突。
schema 26→27 必须停服执行 `scripts/migrate-state-26-27.mjs`，运行时不迁移。
迁移除增加版本字段外，还沿现存 fork 对应的分支父链，将直接指向被替换消息的旧编辑边改为指向公共父消息；不删除消息、运行或账号。跨会话关系、父链环或非用户替代边会中止事务。备份在转换前生成并验证，重复转换拒绝执行。

发布前已完成：架构/Biome/TypeScript 检查、61 个文件的 330 项测试、全部 workspace 构建、嵌入资源构建及 36 项 Web E2E。随后新增真实旧编辑边迁移回归，迁移测试 4 项通过。最终发布入口再次执行检查、全量测试和构建。

`conversation-execution-recovery-design.zh-CN.md` 是后续目标设计，并不表示其中 sessionRevision、独立 replacement 表、质量采用接口及完整设备矩阵均已实现；当前实现使用持久 fork、branchRevision 和 queueRevision。本次修复验收不能替代该完整目标验收。既有性能门禁及尚未执行的 Android 真机矩阵仍以 `release-acceptance.md` 为准，不声称全项目所有功能无缺陷。

发布需核验服务器真实数据副本的迁移、保护账号对象指纹、live/ready、Core/Web 同源提交与资源哈希，Android 必须使用已有正式证书及递增版本。服务器发布编号与备份记录写入 MyServer 运维记录。
